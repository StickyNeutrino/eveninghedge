version: '3.3'

services:
 
  jscrape:
    build: jscrape
    volumes:
    - type: bind
      source: ./data
      target: /data

  database:
    build: db
    environment:
      POSTGRES_PASSWORD: LKFO#*)
